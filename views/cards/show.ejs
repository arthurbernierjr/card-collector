<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= card.title %></title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= card.name %>, Type: <%= card.type %></h1>
    <p>Condition: <%= card.condition %></p>
    <% if (card.cardNum) { %>
    <p>Set/Card Number: <%= card.cardNum %></p>
    <% } %>
    <form
        action="/users/<%= user._id %>/cards/<%= card._id %>?_method=DELETE"
        method="POST"
      >
        <button type="submit">Delete this card.</button>
    </form>
  </body>
</html>
  